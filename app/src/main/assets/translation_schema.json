{
  "type": "object",
  "properties": {
    "summary": {
      "type": "string",
      "description": "A concise summary of the entire supplied scene written in target_lang. Account for alternative branches without treating mutually exclusive events as one continuous path."
    },
    "translations": {
      "type": "array",
      "description": "Contains exactly one translated entry for every input text object.",
      "items": {
        "type": "object",
        "properties": {
          "seq": {
            "type": "integer",
            "minimum": 1,
            "description": "The seq copied unchanged from the corresponding input text object."
          },
          "text": {
            "type": "string",
            "description": "The translated target-language text. Every protected placeholder label must be preserved exactly."
          }
        },
        "required": [
          "seq",
          "text"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "summary",
    "translations"
  ],
  "additionalProperties": false
}